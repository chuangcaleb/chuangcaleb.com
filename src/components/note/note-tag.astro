---
import {Icon} from 'astro-icon/components';
import Tag from '~/components/tag.astro';
import type {NoteTag} from '~/utils/note-metadata';

type Props = NoteTag;

const {icon, label} = Astro.props;
---

<Tag>
	{icon && <Icon name={icon}/>}
	{label}
</Tag>
