---
import { type HTMLAttributes } from "astro/types";

export interface Props extends HTMLAttributes<"ul"> {}

const { class: className, ...props } = Astro.props;
---

<ul class:list={["norm", className]} {...props}>
  <slot />
</ul>

<style>
  * {
    display: flex;
    flex-wrap: wrap;
    gap: var(--size-relative-3);
  }
</style>
