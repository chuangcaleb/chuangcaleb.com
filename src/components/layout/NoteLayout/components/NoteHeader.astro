---
interface Props {
  title: string;
  description?: string;
}

const { title, description } = Astro.props;
---

<h1>{title}</h1>
<p>{description}</p>
<slot />

<style>
  p {
    color: var(--text-faded);
  }
</style>
